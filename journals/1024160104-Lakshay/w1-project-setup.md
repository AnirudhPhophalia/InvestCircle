# Week 1 : InvestCircle Project Setup (Phase 0)

Set up the initial skeleton for the InvestCircle app:

- `frontend/` — React + Vite + Tailwind, tokens (colors, fonts, spacing) copied from our Stitch design export.
- `backend/` — Express + SQLite (`better-sqlite3`), schema empty for now.
- Built the app shell: left sidebar (Discussions / News Reel / Portfolio), top bar with search, light/dark mode toggle.
- Routing wired up with React Router, three placeholder pages.
- Checked it in the browser — nav and dark mode toggle both work, no console errors.

Next: Phase 1 — auth (login/signup).
