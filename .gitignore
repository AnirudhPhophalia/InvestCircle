node_modules/
.env

# build output
frontend/dist/

# local Postgres/SQLite artifacts (backend now uses DATABASE_URL, no file DB is committed)
backend/*.db
backend/*.db-journal
backend/*.db-wal
backend/*.db-shm

# editor/OS
.DS_Store
